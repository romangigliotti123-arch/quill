# Spoken-corpus script for the quill gauntlet.
#
# Read one line at a time, exactly as written. Blank lines and #-comments are
# ignored. The line IS the ground truth, so if you improvise, the WER for that
# clip is measuring your improvisation, not the app.
#
# The vocabulary is deliberate. LibriSpeech is 19th-century audiobook prose and
# tells you nothing about how either app handles the words Roman actually
# dictates: project names, Australian place names, and developer jargon that a
# general language model has never been asked to spell. That is exactly where a
# cloud model with a big vocabulary should beat a small local one, so it is the
# fair place to look for the gap.
#
# 32 lines: 14 vocabulary-loaded, 18 ordinary dictation prose.

# ── project and tool names ────────────────────────────────────────────────────
Push the nxt onboarding manager to Netlify once the Firestore rules are updated.
The graphify workspace rebuilt the knowledge graph across every project folder.
Open Nebula and check whether the cosmic globe still renders the god nodes.
Vesper is the Lunar style wrapper that sits around the blockcraft replica.
I need to check the Firestore security rules before the client codes go live.
The Netlify production build only deploys from main, not from dev.
Send the deposit invoice to the barber shop in Craigieburn this afternoon.
I am based in Melbourne, so most of my clients are in the northern suburbs.
Add a blockcraft tile to the dashboard next to the graphify link.
The Vesper launcher needs the bundled runtime before it can extract anything.
Quill should insert the text without ever touching the clipboard.
Wispr Flow stores every transcript in a local SQLite database.
Check the Firestore console for the onboarding process project.
The Nebula viewer needs a local proxy because the API has no CORS headers.

# ── ordinary dictation prose ──────────────────────────────────────────────────
Can you send me the updated invoice before the end of the week?
I think the hero section needs more contrast and a bit more breathing room.
Let me know if Tuesday afternoon works better for the call.
The client wants the booking form above the fold on mobile.
I spent most of yesterday debugging a permissions issue that turned out to be a typo.
We should probably ship the smaller change first and measure it properly.
There is no point optimising something that nobody has actually complained about.
Remind me to follow up with him on Thursday if I have not heard back.
The whole page loads in under a second now, which is a decent improvement.
I would rather do it properly once than rush it and redo it twice.
Could you double check the pricing table before I send this over?
It turns out the bug only happens when the window is resized quickly.
Let us keep the copy short and let the photography do the work.
The dark mode version needs slightly warmer greys than the light one.
I will push the changes tonight and tag a release in the morning.
Honestly, the second draft reads much better than the first one did.
Make sure the contact form actually sends before we call it finished.
If the numbers do not move after a week, we will try something else entirely.
