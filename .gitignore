.build/
build/
.DS_Store
rig/audio/
rig/out/
*.bin
*.mlmodelc
Quill.app
# macOS hands out "Quill 2.app", "Quill 3.app" when the convenience symlink's
# name is already taken, and those leaked into git status as untracked bundles.
Quill?*.app
diagnostics.txt
.claude/worktrees/

# Written fresh by `handoff receive` on each machine; it is a local artifact,
# not repo content, and committing it would fight the next handoff.
HANDOFF.md
